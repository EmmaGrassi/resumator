import { expect } from 'chai';

import {{ properCase unit }} from '{{ properCase path }}';

describe('{{ properCase unit }}', () => {
  it('should pass cause it is a dummy test', () => {
    expect(true).to.eql(true);
  });
});
