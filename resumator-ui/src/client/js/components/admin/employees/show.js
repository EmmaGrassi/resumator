const React = require('react');

class Show extends React.Component {
  displayName = 'Show';

  render() {
    return (
      <div>
        Admin Employees Show
      </div>
    );
  }
}

module.exports = Show;
