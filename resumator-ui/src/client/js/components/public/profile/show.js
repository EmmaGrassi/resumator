const React = require('react');

class Show extends React.Component {
  render() {
    return (
      <div>
        Public Profile Show
      </div>
    );
  }
}

Show.displayName = 'Show';

module.exports = Show;
