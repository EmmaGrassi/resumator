service:
  image: localhost:5000/resumator-service
  command: -Dresumator.http.context.path=api/
  links:
    - db
db:
  image: postgres
  environment:
    - POSTGRES_USER=resumator
    - POSTGRES_PASSWORD=resumator
    - POSTGRES_DB=resumator
ui:
  image: localhost:5000/resumator-ui
lb:
  image: localhost:5000/resumator-load-balancer
  ports:
    - "8000:80"
  links:
    - ui
    - service
